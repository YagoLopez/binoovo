describe('Test root path', () => {
  it('Loads root path without errors', () => {
    cy.visit('http://localhost:3000')
  })
})

describe('Test root path', () => {
  it('Loads root path without errors', () => {
    cy.visit('http://localhost:3000')
  })
})